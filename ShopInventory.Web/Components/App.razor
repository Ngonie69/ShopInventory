@using Microsoft.AspNetCore.Components.Web

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0, viewport-fit=cover, interactive-widget=resizes-content" />
    <meta name="color-scheme" content="light dark" />
    <base href="/" />

    <!-- PWA Meta Tags -->
    <meta name="theme-color" content="#f8fafc" media="(prefers-color-scheme: light)" />
    <meta name="theme-color" content="#0f172a" media="(prefers-color-scheme: dark)" />
    <meta name="description"
          content="Inventory management system with SAP Business One integration for retail operations in Zimbabwe" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="Shop Inventory" />
    <link rel="manifest" href="/manifest.json" />
    <link rel="apple-touch-icon" href="/images/icons/icon-192x192.png" />

    <ResourcePreloader />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Outfit:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap"
    rel="stylesheet">
    <link rel="stylesheet" href="@Assets["lib/bootstrap/dist/css/bootstrap.min.css"]" />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    @* First of the Nocturne sheets: the palette itself, which the page sheets
       below alias rather than restate. See its header for which ones do. *@
    <link rel="stylesheet" href="@Assets["css/nocturne-tokens.css"]" />
    <link rel="stylesheet" href="@Assets["css/inventory-transfers.css"]" />
    <link rel="stylesheet" href="@Assets["css/transfer-forms.css"]" />
    @* All six sales document forms — the five create/edit screens and
       /invoices/create, which this replaced create-invoice.css to absorb. *@
    <link rel="stylesheet" href="@Assets["css/sales-forms.css"]" />
    <link rel="stylesheet" href="@Assets["css/credit-notes.css"]" />
    <link rel="stylesheet" href="@Assets["css/settings.css"]" />
    <link rel="stylesheet" href="@Assets["css/batch-status.css"]" />
    <link rel="stylesheet" href="@Assets["css/exception-center.css"]" />
    <link rel="stylesheet" href="@Assets["css/customer-portal.css"]" />
    @* Directly after the portal's own sheet, whose palette it aliases: the
       statement page is the one portal page with a design of its own. *@
    <link rel="stylesheet" href="@Assets["css/account-statement.css"]" />
    <link rel="stylesheet" href="@Assets["css/driver-shop-access.css"]" />
    <link rel="stylesheet" href="@Assets["css/mobile-orders.css"]" />
    <link rel="stylesheet" href="@Assets["css/approval-process.css"]" />
    <link rel="stylesheet" href="@Assets["css/business-partners.css"]" />
    @* The admin side of the portal — who has an account, not what they see
       once they are in it — so it is a Nocturne page of its own rather than
       part of customer-portal.css above. *@
    <link rel="stylesheet" href="@Assets["css/customer-portal-accounts.css"]" />
    @* Derived from business-partners.css and sharing its palette — see that
       file's header, and this one's, for what "one system" costs. *@
    <link rel="stylesheet" href="@Assets["css/products.css"]" />
    <link rel="stylesheet" href="@Assets["css/dashboard.css"]" />
    <link rel="stylesheet" href="@Assets["css/sales-rep-dashboard.css"]" />
    <link rel="stylesheet" href="@Assets["css/pod-dashboard.css"]" />
    @* Imported from POD Report.dc.html; aliases the palette in
       nocturne-tokens.css rather than restating it. *@
    <link rel="stylesheet" href="@Assets["css/pod-report.css"]" />
    <link rel="stylesheet" href="@Assets["css/sync-status.css"]" />
    <link rel="stylesheet" href="@Assets["css/whatsapp-console.css"]" />
    <link rel="stylesheet" href="@Assets["css/quotations.css"]" />
    <link rel="stylesheet" href="@Assets["css/sales-orders.css"]" />
    <link rel="stylesheet" href="@Assets["css/invoices.css"]" />
    <link rel="stylesheet" href="@Assets["css/payments.css"]" />
    @* One sheet, four purchase registers — quotations, requests, A/P invoices
       and goods receipt POs. Derived from audit-trail.css; see its header for
       why the four share a sheet and why /purchase-orders does not. *@
    <link rel="stylesheet" href="@Assets["css/purchase-documents.css"]" />
    @* The five purchase forms, straight after the registers they are entered
       from and whose palette they take. Replaces app.css's `.pdoc-create-*`
       block and two inline style blocks. *@
    <link rel="stylesheet" href="@Assets["css/purchase-forms.css"]" />
    <link rel="stylesheet" href="@Assets["css/pods.css"]" />
    <link rel="stylesheet" href="@Assets["css/order-fulfillment.css"]" />
    @* Sibling of business-partners.css, sharing its palette: the audit trail is
       the same page shape. See that file's header for what the sharing costs. *@
    <link rel="stylesheet" href="@Assets["css/audit-trail.css"]" />
    <link rel="stylesheet" href="@Assets["css/local-stock.css"]" />
    @* The admin/ops pages. Each is a derivation rather than an import — there
       are no .dc.html designs for this group — and each sheet's header names
       the sibling it was taken from. *@
    <link rel="stylesheet" href="@Assets["css/mobile-push.css"]" />
    <link rel="stylesheet" href="@Assets["css/notifications.css"]" />
    <link rel="stylesheet" href="@Assets["css/document-templates.css"]" />
    <link rel="stylesheet" href="@Assets["css/backups.css"]" />
    <link rel="stylesheet" href="@Assets["css/security.css"]" />
    <link rel="stylesheet" href="@Assets["css/ai-assistant.css"]" />
    <link rel="stylesheet" href="@Assets["css/document-manager.css"]" />
    <link rel="stylesheet" href="@Assets["css/webhooks.css"]" />
    <link rel="stylesheet" href="@Assets["css/user-activity.css"]" />
    <link rel="stylesheet" href="@Assets["css/user-management.css"]" />
    @* Both merchandiser admin pages, derived from user-management.css above and
       sharing its shape — see that sheet's header. *@
    <link rel="stylesheet" href="@Assets["css/merchandiser.css"]" />
    @* Not a page: the shared Nocturne date field, which carries its own copy of
       the palette because a component cannot see a page's own tokens. *@
    <link rel="stylesheet" href="@Assets["css/nocturne-date-field.css"]" />
    @* Last of the app stylesheets: it re-skins the staff topbar and sidebar on
       the Nocturne design and has to land after app.css's shell rules. *@
    <link rel="stylesheet" href="@Assets["css/nocturne-shell.css"]" />
    <link rel="stylesheet" href="@Assets["ShopInventory.Web.styles.css"]" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" />
    @* Phosphor is the Nocturne design system's icon set (used by /invoices/create).
       Served from jsDelivr, the only icon CDN our Content-Security-Policy allows in
       style-src and font-src — from unpkg the stylesheet and font are both blocked
       and every Phosphor glyph renders as nothing. *@
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@@phosphor-icons/web@2.1.1/src/regular/style.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@@phosphor-icons/web@2.1.1/src/fill/style.css" />
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet" />
    <link href="_content/MudBlazor/MudBlazor.min.css" rel="stylesheet" />
    @* Must stay after MudBlazor: SalesOrders puts .btn-so-gradient on MudButton,
       and .mud-button-* matches it on specificity, so order decides the winner. *@
    <link rel="stylesheet" href="@Assets["css/sales-order-buttons.css"]" />
    <ImportMap />
    @* favicon.svg is the simplified mark, drawn to stay legible at 16-32px. *@
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet @rendermode="new InteractiveServerRenderMode(prerender: false)" />

    <!-- Early theme initialization to prevent flash of unstyled content on iOS -->
    <script>
            (function () {
                try {
                    var storedTheme = localStorage.getItem('app-theme');
                    var theme = storedTheme
                        ? storedTheme.replace(/"/g, '')
                        : (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
                    // Remove quotes if stored as JSON string
                    var htmlEl = document.documentElement;
                    htmlEl.setAttribute('data-theme', theme);
                    htmlEl.setAttribute('data-bs-theme', theme);
                    htmlEl.classList.add(theme + '-theme');

                    // Update color-scheme meta
                    var metaCS = document.querySelector('meta[name="color-scheme"]');
                    if (metaCS) metaCS.content = theme;

                    // Update theme-color meta
                    var metaTC = document.querySelector('meta[name="theme-color"]:not([media])');
                    if (!metaTC) {
                        metaTC = document.createElement('meta');
                        metaTC.name = 'theme-color';
                        document.head.appendChild(metaTC);
                    }
                    metaTC.content = theme === 'dark' ? '#071425' : '#F6F8FB';
                } catch (e) { }
            })();
    </script>
</head>

<body>
    <!-- Inline critical JS functions BEFORE anything else -->
    <script>
            // Download file function - uses Blob for reliable large file support
            window.downloadFile = function (fileName, contentTypeOrBase64, base64Content) {
                let base64Data, contentType;

                if (arguments.length === 2 || base64Content === undefined) {
                    base64Data = contentTypeOrBase64;
                    if (fileName.endsWith('.xlsx')) {
                        contentType = 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet';
                    } else if (fileName.endsWith('.pdf')) {
                        contentType = 'application/pdf';
                    } else if (fileName.endsWith('.csv')) {
                        contentType = 'text/csv';
                    } else {
                        contentType = 'application/octet-stream';
                    }
                } else {
                    contentType = contentTypeOrBase64;
                    base64Data = base64Content;
                }

                const binaryString = atob(base64Data);
                const byteArray = new Uint8Array(binaryString.length);
                for (let i = 0; i < binaryString.length; i++) {
                    byteArray[i] = binaryString.charCodeAt(i);
                }

                const blob = new Blob([byteArray], { type: contentType });
                const url = window.URL.createObjectURL(blob);
                const downloadLink = document.createElement('a');
                downloadLink.href = url;
                downloadLink.download = fileName;
                document.body.appendChild(downloadLink);
                downloadLink.click();
                document.body.removeChild(downloadLink);
                window.URL.revokeObjectURL(url);
            };

            // Print HTML content using a hidden iframe (avoids popup blocker issues in Blazor Server)
            window.printReportHtml = function (htmlContent) {
                var iframe = document.getElementById('_reportPrintFrame');
                if (!iframe) {
                    iframe = document.createElement('iframe');
                    iframe.id = '_reportPrintFrame';
                    iframe.style.position = 'fixed';
                    iframe.style.right = '0';
                    iframe.style.bottom = '0';
                    iframe.style.width = '0';
                    iframe.style.height = '0';
                    iframe.style.border = 'none';
                    document.body.appendChild(iframe);
                }
                var doc = iframe.contentDocument || iframe.contentWindow.document;
                doc.open();
                doc.write(htmlContent);
                doc.close();
                // Wait for content to render, then print
                setTimeout(function () {
                    iframe.contentWindow.focus();
                    iframe.contentWindow.print();
                }, 300);
            };
    </script>
    <script src="js/app.js?v=9"></script>
    <script src="js/passkeys.js?v=1"></script>
    <script src="js/signature-pad.js?v=2"></script>

    <!-- Loading screen shown while Blazor connects -->
    <div id="blazor-loading"
         style="display: flex; justify-content: center; align-items: center; height: 100vh; background: linear-gradient(135deg, #0b0b0b 0%, #151515 45%, #242424 100%); position: fixed; top: 0; left: 0; right: 0; bottom: 0; z-index: 9999;">
        <div style="text-align: center; color: #f5f5f5;">
            <div
                style="width: 48px; height: 48px; border: 3px solid rgba(255,255,255,0.2); border-top-color: #fff; border-radius: 50%; animation: blazor-spin 0.8s linear infinite; margin: 0 auto 1rem;">
            </div>
            <p style="font-family: 'Segoe UI', sans-serif; font-size: 1rem; opacity: 0.8; margin: 0;">Loading...</p>
        </div>
    </div>
    <style>
        @@keyframes blazor-spin {
        to {
        transform: rotate(360deg);
        }
        }
    </style>

    <CascadingAuthenticationState>
        <Routes @rendermode="new InteractiveServerRenderMode(prerender: false)" />
    </CascadingAuthenticationState>
    <ReconnectModal />

    <script src="@Assets["_framework/blazor.web.js"]" autostart="false"></script>
    <script>
        // Start Blazor and hide loading screen once connected
        Blazor.start().then(function () {
        var loader = document.getElementById('blazor-loading');
        if (loader) loader.style.display = 'none';
        }).catch(function () {
        var loader = document.getElementById('blazor-loading');
        if (loader) {
        loader.innerHTML = '<div style="text-align:center;color:#f5f5f5;font-family:Segoe UI,sans-serif;">'
        + '<p style="font-size:1.1rem;margin:0 0 1rem;">Failed to connect to the server.</p>'
        + '<button onclick="location.reload()" style="padding:0.5rem 1.5rem;background:#3b82f6;color:#fff;border:none;border-radius:6px;cursor:pointer;font-size:0.95rem;">Retry</button>'
        + '</div>';
        }
        });
        // Fallback: show retry UI after 20s if Blazor hasn't connected
        setTimeout(function () {
        var loader = document.getElementById('blazor-loading');
        if (loader && loader.style.display !== 'none') {
        loader.innerHTML = '<div style="text-align:center;color:#f5f5f5;font-family:Segoe UI,sans-serif;">'
        + '<p style="font-size:1.1rem;margin:0 0 1rem;">Connection is taking longer than expected.</p>'
        + '<button onclick="location.reload()" style="padding:0.5rem 1.5rem;background:#3b82f6;color:#fff;border:none;border-radius:6px;cursor:pointer;font-size:0.95rem;">Retry</button>'
        + '</div>';
        }
        }, 20000);
    </script>

    <script src="_content/MudBlazor/MudBlazor.min.js"></script>

    <!-- Service Worker Registration -->
    <script>
        if ('serviceWorker' in navigator) {
        window.addEventListener('load', function () {
        navigator.serviceWorker.register('/service-worker.js')
        .then(function (registration) {
        console.log('ServiceWorker registered: ', registration.scope);
        })
        .catch(function (error) {
        console.log('ServiceWorker registration failed: ', error);
        });
        });
        }
    </script>
</body>

</html>
