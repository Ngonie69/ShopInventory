<span class="kf-status-badge kf-status-badge--@Tone">@Text</span>

@code {
    [Parameter, EditorRequired] public string Text { get; set; } = string.Empty;
    [Parameter] public string Tone { get; set; } = "neutral";
}
